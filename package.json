{
  "private": true,
  "scripts": {
    "build": "eslint src && spago build --censor-stats --strict --ensure-ranges --pedantic-packages",
    "test": "spago test --offline --censor-stats --strict --pedantic-packages"
  },
  "devDependencies": {
    "eslint": "^9.12.0"
  }
}
